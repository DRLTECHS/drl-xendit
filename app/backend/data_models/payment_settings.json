{
    "title": "payment_settings",
    "type": "object",
    "properties": {
        "id": {
            "type": "integer",
            "description": "Primary key, auto-increment"
        },
        "user_id": {
            "type": "string",
            "description": "Admin user ID who owns this setting"
        },
        "provider": {
            "type": "string",
            "description": "Payment provider name (e.g., xendit, stripe)"
        },
        "public_key": {
            "type": "string",
            "description": "Public API key (encrypted)"
        },
        "secret_key": {
            "type": "string",
            "description": "Secret API key (encrypted)"
        },
        "is_active": {
            "type": "boolean",
            "description": "Whether this payment setting is active"
        },
        "environment": {
            "type": "string",
            "description": "Environment: sandbox or production"
        },
        "created_at": {
            "type": "string",
            "format": "date-time",
            "description": "Creation timestamp"
        },
        "updated_at": {
            "type": "string",
            "format": "date-time",
            "description": "Last update timestamp"
        }
    },
    "required": [
        "id",
        "user_id",
        "provider"
    ]
}